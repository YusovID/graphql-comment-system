Создать пост:

mutation CreatePost{
  createPost(input:{
    author:"Test"
    title: "Test"
    content:"Test" 
    allowComments:true
  }) {
    id
  }
}

Получить пост по ID:

query GetPost{
  post(id: "1") {
    id
    author
    title
    content
    allowComments
  }
}

Подключение к базе данных:

psql -U {username} -d {database_name}